import { getById } from 'e2e/helpers'

export const citiesIcon = getById('tabBarIcon-cities').atIndex(0)
export const homeIcon = getById('tabBarIcon-home').atIndex(0)
export const membersIcon = getById('tabBarIcon-members').atIndex(0)
